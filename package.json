{
  "name": "rescueroute-ai-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript sources for the RescueRoute AI medical-team interface.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
